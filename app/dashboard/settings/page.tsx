import { ConfigCenterApp } from '@/components/config-center/ConfigCenterApp';

export default function SettingsPage() {
  return (
    <div className="mx-auto max-w-[min(100%,1400px)] space-y-4 px-1 pb-8 sm:px-0">
      <ConfigCenterApp />
    </div>
  );
}
